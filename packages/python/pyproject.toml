# File: silkllm-sdks/packages/python/pyproject.toml
[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "silkllm"
version = "1.0.0"
description = "One unified API for OpenAI, Anthropic, Google, DeepSeek, and xAI with simple fiat billing."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = ["httpx>=0.27.0"]

[project.urls]
Homepage = "https://getsilkllm.com"
Documentation = "https://getsilkllm.com/docs"

# EOF silkllm-sdks/packages/python/pyproject.toml
